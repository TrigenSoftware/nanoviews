- hydration provider / etc? - @nano_kit/next @nano_kit/next-router integrations
  https://github.com/effector/next

- wildcard -> splat? shorter and easer to type
- cookies / headers to store
- cache handler
- server cache
- Compression
  https://www.npmjs.com/package/vite-plugin-compression2
  https://universal-middleware.dev/middlewares/sirv
  https://github.com/lukeed/sirv/tree/main/packages/sirv#api

- render to stream with data streaming
- @nano_kit/framework ?

## Kida/Agera

- immutability helpers? / make work with $data facet more comfortable
- injectable class?
- lazy scope to start and stop methods? for example for resort in loop
- later: resort effects

## 🔥🔥🔥 Query 🔥🔥🔥

- map data / default data
- synced cache between tabs

## Router

- add payload to push, replace to store it in location state, for example mark transition without scroll, instead of compare prev route and next
- dynamic redirect in storesToPreload and static in pages structure
- page status codes
- onBeforeLeave / blocker https://reactrouter.com/api/hooks/useBlocker
- scroll restoration ssr
- loadable+react.lazy like util

## React Router

- mark current/active links?

## intl

## cookies

## Nanoviews

- fix TruthyValueOrSignal type, return signal if signal, not accessor

- wrap into untracked needed parts
- for loop array or empty check for else
- rework return slot$ to look like element/component? fn.prop is faster than {f,p}
- util to transform static props to signal props?
- rename decide? export as public - as Dynamic in solidjs
- typed effectattrs <T>
- additional arg for record in for$? as$
- controls addEventListener to events system
- static index for untracked loop?

for_($items)(
  as_(record, ($item) => li()($item.$name))
)

for_($items)(
  asRecord(($item) => li()($item.$name))
)

## Perf

- !== undefined is faster
- prop: undefined is faster
- for let i faster than array methods
- push(...arr) is slow
- noop faster than check
- getter setter are slow (not in class)
- Reflect.get is slow
- arr.length = 0 is slower than arr = []

## Long distance plans

- html + head components
- universal dom
- ssr
- hydration
- svg ?
- error boundaries
- hmr
- COMPILER to compete with svelte and solid

### SSR

- setAttribute
- removeAttribute
- on*
- createElement
- insertBefore
- removeChild
- createTextNode
- data
- attachShadow
- *prop
- style
  - setProperty
  - removeProperty
- portals
- innerHTML

## Drafts

```
handler = {
  k: undefined,
  get(target, key) {
      if (key[0] === '$') {
        const skey = key.slice(1)
        const keys = this.k

        keys[keys.indexOf(skey)] = key

        return { signal: target[skey] }
      }

      return target[key]
  },
  ownKeys() {
      return this.k
  },
}

function props$(props) {
    const propsHandler = Object.create(handler)

    propsHandler.k = Reflect.ownKeys(props)

    return new Proxy(props, propsHandler)
}

obj = props$({
	a: 1,
	b: '',
	c: true,
	d: null
})
```

```
const $onNumpadClick = event(event => event.currentTarget.dataset.payload)
const $onSubmit = event()

effect(() => {
  const num = $onNumpadClick()

  if (num !== null) {
    console.log('Numpad clicked', num)
  }
})

effect(() => {
  if ($onSubmit()) {
    submitData()
    $onSubmit(null)
  }
})

button({
  onClick: $onNumpadClick
  'data-payload': 5
})('Click!')
```

```
query<[city: City | undefined], Weather | null>(...)
->
query<(city: City | undefined) => Weather | null>(...)

queryKey<[postId: number], Post | null>('post')
->
queryKey<(postId: number) => Post | null>('post')

operationKey<[], [prompt: string], Post>('postGeneration')
->
operationKey<() => Post, [prompt: string]>('postGeneration')
or
operationKey<() => Post, (prompt: string) => void>('postGeneration')
or
operationKey<[], (prompt: string) => Post>('postGeneration')
operationKey<[postId: number], (prompt: string) => Post>('postGeneration')
```
